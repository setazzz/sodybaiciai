{% extends 'base.html.twig' %}

{% block body %}
            <div>
                <ul>
                    {% for item in list %}
                        {#<a href="{{ path('details', {'id': item.id}) }}" class="box-link">#}
                            {{ include('catalog/box.html.twig') }}
                        {#</a>#}
                    {% endfor %}
                </ul>
            </div>

        </div>
    </div>
{% endblock %}
